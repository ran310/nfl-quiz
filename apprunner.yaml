version: 1.0
runtime: python311
build:
  commands:
    build:
      - echo "Build step complete ... nothing to do here"
run:
  pre-run:
    - echo "Pre-run step started"
    - pip3 install --no-cache-dir -r requirements.txt
    - echo "Pre-run step complete"
  command: "python3 -m gunicorn --bind 0.0.0.0:8080 app:app"
  network:
    port: 8080
