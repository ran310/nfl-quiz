version: 1.0
runtime: python311
build:
  commands:
    build:
      - python -m pip install --no-cache-dir -r requirements.txt
run:
  command: "gunicorn --bind 0.0.0.0:8080 app:app"
  network:
    port: 8080
